server:
  port: 8200
spring:
  application:
    name: demo-classes
  profiles:
    active: 'dev'
  # 配置文件加载顺序：bootstrap>远程配置中心配置文件>本地配置文件
  cloud:
    nacos:
      server-addr: 'localhost:8848'
hujtb:
  plugin:
    page:
      enable: true
